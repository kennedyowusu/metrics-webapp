import { useState, useEffect} from 'react'
import { useSelector, useDispatch } from 'react-redux'

const HomePage = () => {
 const { searchData, setSearchData} = useS
  return (
    <div>
      
    </div>
  )
}

export default HomePage
